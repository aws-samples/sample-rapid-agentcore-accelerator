[project]
name = "customer-support-mcp-server"
version = "0.1.0"
description = "Customer support MCP server with tools for AgentCore Runtime"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp==1.28.1",
]
